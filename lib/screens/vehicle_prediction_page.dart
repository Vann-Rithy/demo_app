import 'package:flutter/material.dart';

class VehiclePredictionPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Vehicle Prediction'),
      ),
      body: Center(
        child: Text('Vehicle Prediction Page'),
      ),
    );
  }
}
