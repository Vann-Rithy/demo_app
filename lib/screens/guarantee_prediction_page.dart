import 'package:flutter/material.dart';

class GuaranteePredictionPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Guarantee Prediction'),
      ),
      body: Center(
        child: Text('Guarantee Prediction Page'),
      ),
    );
  }
}
