import 'package:flutter/material.dart';

class HousePredictionPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('House Prediction'),
      ),
      body: Center(
        child: Text('House Prediction Page'),
      ),
    );
  }
}
