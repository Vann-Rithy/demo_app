import 'package:flutter/material.dart';

class readpage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('អានអត្ថបទ'),
      ),
      body: Center(
        child: Text('អានអត្ថបទ'),
      ),
    );
  }
}
