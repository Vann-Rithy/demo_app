import 'package:flutter/material.dart';

class shoppage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('គ្រឿងបរិក្ខារ'),
      ),
      body: Center(
        child: Text('គ្រឿងបរិក្ខារ'),
      ),
    );
  }
}
